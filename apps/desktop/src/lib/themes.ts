/**
 * Theme tokens moved into @scuttlarr/tui (2026-08-16): the kit's workbench and
 * both apps consume them, so the kit owns them. This shim keeps existing
 * desktop imports stable.
 */
export * from '@scuttlarr/tui'
