# scuttlarr theme: {{ name }} — rendered, do not edit. Sourced by the base .p10k.zsh
# before its segment styling, so a lean p10k config can say `$accent` / `$comment`
# and retint with the theme on the next prompt.
local background='{{ background }}'
local foreground='{{ foreground }}'
local comment='{{ muted }}'
local accent='{{ accent }}'
local host='{{ accent }}'
local red='{{ red }}'
local orange='{{ orange }}'
local yellow='{{ yellow }}'
local green='{{ green }}'
local cyan='{{ cyan }}'
local blue='{{ blue }}'
local purple='{{ magenta }}'
local pink='{{ bright_magenta }}'
