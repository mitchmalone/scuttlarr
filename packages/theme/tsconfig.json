{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Node runs bin/generate.ts directly (type stripping), which needs explicit .ts specifiers.
    "allowImportingTsExtensions": true,
    "lib": ["ES2022"],
    "types": ["node"]
  },
  "include": ["src", "bin"]
}
